# Brent Demo

This file exists purely to trigger a demo pull request for Brent workflows.

When this PR is opened as **non-draft**, the `demo-openai-greeter` and
`demo-claude-greeter` workflows post a friendly greeting comment.
